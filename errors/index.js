const Unauthorized = require('./unauthorized');
const BadRequest = require('./bad-request');
const customAPIError = require('./custom-error');

module.exports = { Unauthorized, BadRequest, customAPIError };
